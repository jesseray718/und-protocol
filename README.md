# und — Universal Native Descriptor

**Dense language for offline machines.**  
**Physics decides what code is allowed to touch the real world.**

Token compression 5.5×–8.2×  
Zero-heap C for ESP32 / STM32  
Pure Python (stdlib only)  
R_equilibrium = 0.8158

Natural language is wasteful on the edge.  
`und` replaces filler with high-entropy frames.  
A 7B model on a $5 board carries the context of a much larger model.

## Core Frame
If ΔR > 0.8158 the frame is rejected.  
Hallucinated relay commands never reach GPIO.

## Files

- `und_protocol.py` — Python encoder + decoder
- `und_decoder.h` + `und_decoder.cpp` — zero-heap C
- `examples/arduino_esp32_example.ino`

## Quick Start

```bash
python3 und_protocol.py
Commercial Path
Software foundation for Node Zero offline controllers.
Paid thermal blueprint: https://github.com/jesseray718/black-locust-rmh
CC-BY-SA-4.0 / GPL-3.0 · No patents
